# simple-tui

最小可运行的 Node + React 终端界面示例，基于 `ink`。

## 运行环境

- Node.js 18+
- npm 9+

## 安装与启动

```bash
npm install
npm start
```

## 交互说明

- `↑` / `↓`：切换菜单项
- `Enter`：执行当前菜单项
- `i`：进入输入模式
- 输入模式下键入文本，`Enter` 提交，`Esc` 取消
- `q`：退出程序
